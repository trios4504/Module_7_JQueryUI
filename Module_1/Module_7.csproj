<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>netcoreapp3.1</TargetFramework>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(RunConfiguration)' == 'Module_1' " />
  <PropertyGroup Condition=" '$(RunConfiguration)' == 'Module_4' " />
  <PropertyGroup Condition=" '$(RunConfiguration)' == 'Module_5' " />
  <PropertyGroup Condition=" '$(RunConfiguration)' == 'Module_6' " />
  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Razor.RuntimeCompilation" Version="3.1.12" />
  </ItemGroup>
</Project>
